export  let  sts: string = 'db';
export let nsts: Boolean = false;
//export let tposts  = [0,1] as any;

export function setValue(newValue: string) {
  sts = newValue;
}
export function setToken(newnsts: Boolean) {
  nsts = newnsts;
}
/* export function setPosts(newposts: []) {
  tposts = newposts;
} */
